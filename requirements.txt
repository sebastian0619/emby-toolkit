# requirements.txt (最终容器化版本)

# --- Web 框架与服务器 (核心) ---
Flask>=2.0
gevent           # 高性能异步网络库，我们的服务器核心
gevent-websocket # 为 gevent 添加 WebSocket 支持
websocket-client # 【【【 新增：用于代理连接到远程 WebSocket 服务器 】】】

# --- 核心功能 ---
requests         # 用于发送所有HTTP请求
beautifulsoup4   # 用于解析网页HTML
lxml             # [必须] beautifulsoup4 的高性能解析器
translators      # 用于翻译演员名和角色名
pypinyin         # 用于处理人名拼音
concurrent-log-handler # 并发日志处理器

# --- 定时任务 ---
APScheduler
pytz             # APScheduler 的时区依赖
croniter

# --- AI 翻译功能 ---
openai           # 用于 OpenAI 及兼容的API (如硅基流动)
zhipuai          # 用于智谱AI
google-generativeai # 用于Gemini
 
# --- 一键更新 ---
docker

# --- 封面生成 ---
numpy
Pillow
PyYAML